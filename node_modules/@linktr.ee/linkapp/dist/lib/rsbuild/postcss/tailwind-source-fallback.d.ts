import type { PluginCreator } from 'postcss';
/**
 * Ensures every processed CSS file has at least one `@source` directive so that
 * Tailwind v4 scans user files even if the project predates the migration.
 */
interface TailwindSourceFallbackOptions {
    projectRoot: string;
    glob?: string;
}
export declare function createTailwindSourceFallback(options: TailwindSourceFallbackOptions): PluginCreator<void>;
export {};
//# sourceMappingURL=tailwind-source-fallback.d.ts.map