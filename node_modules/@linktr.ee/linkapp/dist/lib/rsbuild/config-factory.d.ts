import type { RsbuildConfig } from '@rsbuild/core';
/**
 * Environment Variables:
 * - Rsbuild automatically injects process.env.NODE_ENV
 * - Use source.define to inject custom build-time constants
 * - For runtime env vars, prefix with PUBLIC_ (e.g., PUBLIC_API_KEY)
 *   and they will be available as import.meta.env.PUBLIC_API_KEY
 *
 * PostCSS & Tailwind CSS:
 * - Rsbuild loads postcss.config.* files from the user's project root
 * - For Tailwind CSS v4, users should create postcss.config.mjs with @tailwindcss/postcss
 * - We let Rsbuild handle loading user's PostCSS config to avoid version conflicts
 */
interface RsbuildConfigOptions {
    mode: 'development' | 'production';
    port?: number;
    host?: string;
    open?: boolean;
    sourcemap?: boolean;
    profile?: boolean;
    compress?: boolean;
}
export declare function createRsbuildConfig(options: RsbuildConfigOptions): RsbuildConfig;
export {};
//# sourceMappingURL=config-factory.d.ts.map